services:
  loja:
    build: .
    ports:
      - "3000:3000"
    environment:
      APP_ENV: container
      DB_PROVIDER: sqlite
      SQLITE_PATH: /app/data/loja.db
    volumes:
      - loja-data:/app/data
volumes:
  loja-data:
